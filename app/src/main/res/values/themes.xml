<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MiPanaderia" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/clean_yellow</item>
        <item name="colorPrimaryVariant">@color/skin</item>
        <item name="colorOnPrimary">@color/font_black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/deep_brown</item>
        <item name="colorSecondaryVariant">@color/medium_brown</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorOnPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.MiPanaderia.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MiPanaderia.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MiPanaderia.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>